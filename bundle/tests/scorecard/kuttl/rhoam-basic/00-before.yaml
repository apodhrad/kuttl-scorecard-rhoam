apiVersion: kuttl.dev/v1beta1
kind: TestStep
delete:
# Delete the pod with testing container in the current namespace
- apiVersion: v1
  kind: Pod
  name: rhoam-test-container
# commands:
  #- script: envsubst < rbac.yaml | kubectl apply -f -
  # - script: kubectl apply -f test-pod-template.yaml